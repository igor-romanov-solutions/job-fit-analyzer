spring:
  application:
    name: JobFitAnalyzer

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

openai:
  api:
    key: ${OPENAI_API_KEY:}
